services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      DB_PATH: /app/data/data.sqlite
      JWT_SECRET: change-me-in-production
    volumes:
      - app-data:/app/data

volumes:
  app-data:
